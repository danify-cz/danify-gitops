apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd
data:
  service.webhook.discord: |
    url: $discord-webhook-url
    headers:
    - name: Content-Type
      value: application/json

  template.app-deployed: |
    webhook:
      discord:
        method: POST
        body: |
          {"content":"✅ **{{.app.metadata.name}}** deployed to {{.app.spec.destination.namespace}}","embeds":[{"color":3066993,"fields":[{"name":"Revision","value":"{{.app.status.sync.revision}}","inline":true}]}]}

  template.app-sync-failed: |
    webhook:
      discord:
        method: POST
        body: |
          {"content":"❌ **{{.app.metadata.name}}** sync failed in {{.app.spec.destination.namespace}}"}

  template.app-health-degraded: |
    webhook:
      discord:
        method: POST
        body: |
          {"content":"⚠️ **{{.app.metadata.name}}** health degraded - status: {{.app.status.health.status}}"}

  trigger.on-deployed: |
    - when: app.status.operationState.phase in ['Succeeded']
      send: [app-deployed]

  trigger.on-sync-failed: |
    - when: app.status.operationState.phase in ['Error', 'Failed']
      send: [app-sync-failed]

  trigger.on-health-degraded: |
    - when: app.status.health.status == 'Degraded'
      send: [app-health-degraded]

  # Global subscriptions - all apps get notifications
  subscriptions: |
    - recipients:
      - discord
      triggers:
      - on-deployed
      - on-sync-failed
      - on-health-degraded
